var fib = function(max) {
    var first = 1;
    var second = 2;
    var sum = second;
    while (second < max) {
        var next = first + second;
        if(next % 2 === 0) {
            sum += next;
        }
        first = second;
        second = next;
    }
    console.log(sum);
};
